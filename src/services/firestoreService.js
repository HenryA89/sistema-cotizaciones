// Firebase removed
// import {
//   collection,
//   doc,
//   addDoc,
//   updateDoc,
//   deleteDoc,
//   Timestamp,
// } from "firebase/firestore";

export const getCollectionPath = (colName) => {
  // Firebase removed
  return null;
};

export const saveItem = async (col, data) => {
  // Firebase removed
  console.log("Firebase removed, cannot save", col, data);
};

export const updateItem = async (col, id, data) => {
  // Firebase removed
  console.log("Firebase removed, cannot update", col, id, data);
};

export const deleteItem = async (col, id) => {
  // Firebase removed
  console.log("Firebase removed, cannot delete", col, id);
};
